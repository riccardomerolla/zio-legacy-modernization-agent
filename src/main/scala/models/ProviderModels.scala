package models

type AIProvider = config.entity.AIProvider
val AIProvider = config.entity.AIProvider

type AIResponse = config.entity.AIResponse
val AIResponse = config.entity.AIResponse

type AIProviderConfig = config.entity.AIProviderConfig
val AIProviderConfig = config.entity.AIProviderConfig

type GeminiResponse = config.entity.GeminiResponse
val GeminiResponse = config.entity.GeminiResponse
